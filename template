This document serves to provide proof that {{ owner.name }} <{{ owner.email }}> is the owner of "{{ owned.desc }}" at {{ owned.domain }}. This attestation is made {{ attest.date }} ({{ attest.tz }}), and will be housed in perpetuity at/under {{ attest.home }}.

{{ attest.uniqueData.base64 }}

The above {{ attest.uniqueData.lengthBytes }} bytes of base64-encoded entropy has the {{ attest.hash.algo }} of {{ attest.uniqueData.hash }} (when calculated on the decoded raw binary data).

Generated by {{ attest.package.name }} {{ attest.package.version }}, source file {{ attest.hash.algo }}: {{ attest.hash.hex }}
